# Example DrWatson script
using DrWatson
@quickactivate "{{project_name}}"

using DataFrames
using Statistics

# Load data
# df = load(datadir("raw", "data.csv"))

# Process data
# results = process_data(df)

# Save results
# wsave(datadir("processed", "results.jld2"), results)

println("Script completed successfully")
